export type ProductItem = {
  id: number;
  name: string;
  price: string;
  img: string;
};

export type CartItem = {
  id: number;
  name: string;
  price: number;
  image: string;
  quantity: number;
};
